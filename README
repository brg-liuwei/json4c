A json lib for C

First, you need to install CMake.
    http://www.cmake.org/

Compile.
    1) CMake .
    2) make
    then, we can run bin/js_test

Install.
    You can modify the CMakeLists.txt to compile the project to xxx.a or xxx.so

ReCompile.
    1) sh cleanup.sh
    2) CMake .
    3) make

